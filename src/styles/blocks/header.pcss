.header {
  padding: 64px 0;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.header__logo-link {
  font-family: 'Open Sans';
  font-size: 36px;
  font-weight: 800;
  line-height: 1;
  color: $slate;
}
